
# Core dependencies
numpy>=1.20
astropy>=5.0
scipy>=1.7
scikit-image>=0.21
photutils>=1.5
tqdm>=4.65
Pillow>=9.0

# Optional GPU dependencies are moved to `requirements-gpu.txt` to avoid
# failing CI installs on runners without CUDA.
