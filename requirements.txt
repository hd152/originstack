
# Core dependencies
numpy>=1.20
astropy>=5.0
scipy>=1.7
scikit-image>=0.21
photutils>=1.5
tqdm>=4.65
Pillow>=9.0
psutil>=5.9

# Optional dependencies (install separately as needed):
#   sep             — fast C-backend source extraction (~5-10x faster than DAOStarFinder in Phase 1)
#   opencv-python   — malvar/vng debayer, bilateral denoising (--debayer-method malvar/vng, --denoise-bilateral)
#   PyWavelets      — wavelet denoising (--denoise)
#   cupy-cudaXXx    — GPU acceleration (--use-gpu); see requirements-gpu.txt
#   astroquery      — plate solving (--plate-solve); see requirements-astrometry.txt
